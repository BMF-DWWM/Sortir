{% extends 'base.html.twig' %}

{% block body %}
    {% if app.user  %}
        <h1> Hello {{ app.user.nom }}</h1>
    {% else %}
        <h1>Bonjour visiteur</h1>
    {% endif %}
    <div class="form">
        {{ form_start(createVilleForm) }}

        {{ form_widget(createVilleForm) }}
        <button>Create!</button>
        {{ form_end(createVilleForm) }}
    </div>

    <a href="{{ path('admin_view') }}" title="retour">Retour</a>
{% endblock %}

{% block title  %}{{ parent() }} | Home {% endblock %}