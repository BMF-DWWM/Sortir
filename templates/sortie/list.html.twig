{% extends 'base.html.twig' %}

{% block body %}
    {% if app.user  %}
        <h1> Hello {{ app.user.nom }}</h1>
    {% else %}
        <h1>Hello MotherFucker</h1>
    {% endif %}
    <div>
        {% for sortie in sorties %}
        <article>
{#            <a href="{{ path('sortie_details', {'id' : sortie.id }) }}" title="View sorties detail">#}
                {{ sortie.nom }}
            </a>
        </article>
        {% endfor %}
    </div>
{% endblock %}

{% block title  %}{{ parent() }} | Home {% endblock %}